{
  "name": "backend",
  "version": "0.0.0",
  "description": "JSON-SERVER example",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --port 4000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
